spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cqoj
    username: root
    password: root
  redis:
    database: 4
    host: localhost
    port: 6379
    timeout: 5000

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 如果有需要上传头像，记得在阿里云对象存储里获取到这些信息
aliyun:
  oss:
    access-key: XXX
    key-secret: XXX
    bucket-name: XXX
    endpoint: XXX
